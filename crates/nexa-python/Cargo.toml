[package]
name = "nexa-python"
version = "0.1.0"
edition = "2021"
description = "Future Python bindings for NexaCore (stub)"

[dependencies]
# pyo3 = { version = "0.22", features = ["extension-module"] }
# nexa-core = { workspace = true }

[lib]
# crate-type = ["cdylib"]
