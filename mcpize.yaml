version: 1
name: nexacore
description: "Universal representation runtime — encode any data into high-dimensional holographic hypervectors, compute directly in encoded space, and decode back."
runtime: container
build:
  dockerfile: Dockerfile
startCommand:
  type: stdio
